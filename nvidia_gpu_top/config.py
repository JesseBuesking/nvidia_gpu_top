MIN_TEMP = 40  # minimum gpu temperature (a guesstimate)
MAX_TEMP = 90  # maximum gpu temperature (a guesstimate)
MIN_FAN = 0  # minimum fan speed percent
MAX_FAN = 100  # maximum fan speed percent
THRESHOLD_LOW = .45  # marks the point where good values become ok
THRESHOLD_HIGH = .75  # marks the point where ok values become bad
